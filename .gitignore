# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local-only demo access credentials — never commit
DEMO-CREDENTIALS.local.md

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment files
.env
.env.local
.env.*.local

# Vercel
.vercel
.env*.local

# Supabase
supabase/.temp/

# Playwright MCP scratch (sessions + auto-captured screenshots)
.playwright-mcp/

# Loose screenshots dropped at repo root by verification runs
/*.png
!/favicon.png

# Client BI summary references (PII) — keep local, never commit
docs/BI SUMMARY *.docx
docs/BI SUMMARY *.DOCX
